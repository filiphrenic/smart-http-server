package hr.fer.zemris.custom.scripting.exec.enums;

import hr.fer.zemris.custom.scripting.exec.ValueWrapper;

/**
 * Used to represent if the object is a integer or double in {@link ValueWrapper} class.
 * 
 * @author Filip Hrenić
 * @version 1.0
 */
public enum Type {
    INTEGER, DOUBLE
}
